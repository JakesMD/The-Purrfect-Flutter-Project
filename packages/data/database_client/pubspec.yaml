name: pdatabase_client

environment:
  sdk: ">=3.1.0 <4.0.0"

dependencies:
  drift: ^2.18.0
  drift_dev: ^2.18.0
  flutter:
    sdk: flutter
  pcore:
  ppub:
  sqlite3: ^2.4.3
  sqlite3_flutter_libs: ^0.5.0

dev_dependencies:
  build_runner: ^2.4.10
  ppub_dev:
